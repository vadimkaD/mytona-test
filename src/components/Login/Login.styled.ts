import styled from "styled-components";
import { Row, Form } from "antd";

export const LoginRow = styled(Row)`
  min-height: 100vh;
`;

export const FormWithReducedMarginBottom = styled(Form)`
  .ant-form-item {
    margin-bottom: 12px;
  }
`;
